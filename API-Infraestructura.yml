server:
  port: 0
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/db_infraestructura
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
    hikari:
      register-mbeans: true
      maximum-pool-size: 5
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  boot:
    admin:
      client:
        url: http://localhost:8089
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
      show-components: always
  tracing:
    sampling:
      probability: 1.0
eureka:
  instance:
    instance-id: ${spring.application.name}-${random.uuid}
    health-check-url-path: /actuator/health
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
